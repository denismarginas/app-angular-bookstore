# Deployment

The Vercel + Supabase deployment path that used to be documented here has been removed for now, to keep the project simple (local JSON files only, no external provider).

For deploying the demo for free, see `DEPLOYMENT-AZURE.md`.

This file is safe to delete.
